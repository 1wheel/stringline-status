{
  "dependencies": {
    "gtfs-realtime-bindings": "^0.0.4",
    "scrape-stl": "^1.0.3"
  },
  "scripts": {
    "deploy": "cd .. && rsync -a --omit-dir-times --no-perms --exclude node_modules --exclude serve/raw-data --exclude .git --exclude remote-games --exclude chart/parsed-data stringline-status/ demo@roadtolarissa.com:stringline-status/",
    "start": "node server/index.js"
  }
}
